#ifndef TENSOR_CAST_H
#define TENSOR_CAST_H

///////////////////////////////////////////////////////////////////////////////

// Cast a tensor to a different type, return FALSE if cast is not supported
// or an overflow occurs
bool tensor_cast_op(tensor_pool_t* pool,tensor_t* t);

///////////////////////////////////////////////////////////////////////////////

#endif
